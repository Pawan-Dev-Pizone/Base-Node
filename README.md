# Infomation tab
# middleware directory is working as service here
# module type is used here rather then commanjs approach

# Start the program
    -> npm start (default start)
    -> npm run dev (nodemon)

# using sequelizer ORM for db connection with Ms Sql
    -> you have to seperatly install the nom for mssql and sequelize
    ->  
